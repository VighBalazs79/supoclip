[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["cd backend && pip install uv && uv sync"]

[start]
cmd = "cd backend && uv run uvicorn src.main_refactored:app --host 0.0.0.0 --port $PORT"
