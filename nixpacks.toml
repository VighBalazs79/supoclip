[phases.setup]
nixPkgs = ["python311", "uv", "ffmpeg"]

[phases.install]
cmds = ["cd backend && uv sync --no-cache"]

[start]
cmd = "cd backend && uv run uvicorn src.main_refactored:app --host 0.0.0.0 --port $PORT"
