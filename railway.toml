[build]
builder = "nixpacks"
buildCommand = "cd backend && pip install uv && uv sync"

[deploy]
startCommand = "cd backend && uv run uvicorn src.main_refactored:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
